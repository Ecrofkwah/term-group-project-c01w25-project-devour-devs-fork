version: '3.8'
services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: myapp-backend
    ports:
      - "3000:3000"
    restart: always

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: myapp-frontend
    ports:
      - "5173:5173"
    restart: always
